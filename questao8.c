As expressões p = mat + 1 (nesse caso está caminhando
um tamanho de ponteiro para int) e x = (*mat)++(incrementa 
o conteúdo da matriz),pois nos demais exemplos(p = mat++ e 
p = ++mat) não é válida a forma que está sendo incrementado
o vetor mat.
